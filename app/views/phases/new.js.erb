
$('#newPhaseModal .modal-content').html("<%= j render 'new', phase: @phase %>");
$('#newPhaseModal').modal('show');
$('.modal-header .close').click(function() {
     $('.modal').modal('hide');
});
 $(document).ready(function() {
    $('#completed-checkbox').on('change', function() {
      $('#completed-date-field').prop('disabled', this.checked);
    });
  });
